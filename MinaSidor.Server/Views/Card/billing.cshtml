<style>
    body {
        font-family: Arial, sans-serif;
        margin: 20px;
    }

    h2 {
        color: #333;
    }

    button {
        background-color: #4CAF50;
        color: white;
        padding: 10px 15px;
        margin: 5px;
        border: none;
        border-radius: 3px;
        cursor: pointer;
        transition: background-color 0.3s;
    }

        button:hover {
            background-color: #45a049;
        }

    #logBox {
        margin-top: 20px;
        border: 1px solid #ccc;
        height: 200px;
        overflow-y: scroll;
        padding: 10px;
        background-color: #f5f5f5;
    }

        #logBox div {
            margin-bottom: 5px;
        }
</style>


<h2>Fakturering , välj en av följande val</h2>

    <script type="text/javascript">
        function addLog(message) {
            var logBox = document.getElementById('logBox');
            var logEntry = document.createElement('div');
            logEntry.textContent = message;
            logBox.appendChild(logEntry);
        }
    </script>

    <!-- Knappar för olika handlingar -->
    <div>
        <button onclick="window.location.href='@Url.Action("Faktureringsperiod", "card")'">Period Avtal</button>
    <button onclick="window.location.href='@Url.Action("FaktureringsAvtalOchMix", "card")'">Mix Avtal</button>
    <button onclick="window.location.href='@Url.Action("MikroAvtal", "card")'">Mikro Avtal</button>
    </div>

    <!-- Box för att visa loggar -->
    <div id="logBox"></div>
